# 轻量化网络安全模拟演示平台 — 技术设计文档

> **版本**：V1.0  
> **日期**：2026-07-01  
> **输入**：[网络安全模拟演示平台-开发需求文档.md](e:\网络安全模拟演示平台-开发需求文档.md)  
> **定位**：技术方案设计（编码前评审），覆盖架构、模块拆解、组件树、数据流、目录结构等  

---

## 0. 设计总览

本平台定位为 **内网单机 Docker 部署** 的轻量化网络安全攻防演示工具。核心设计思想：

1. **前后端分离 + 容器化**：Vue 3 前端 → Nginx 反代 → FastAPI 后端 → Docker 仿真网络
2. **插件化攻击/防御引擎**：攻击脚本和防御规则均为独立模块，平台启动时自动发现注册
3. **严格映射约束**：攻击-设备-防御产品的三元关系由 `attack-defense-mapping.json` 强制约束，代码层面不做隐式推断
4. **三屏联动 + 实时推送**：拓扑画布、策略面板、设备详情三区通过 WebSocket 联动
5. **演示优先**：深色科技风 UI、攻击动画粒子、防御光环、实时流量光效

---

## 1. 总体架构图

```
                            ┌──────────────────────────┐
                            │  用户浏览器（PC / 移动）    │
                            │  Vue 3 + Vite + Pinia     │
                            │  AntV X6 + ECharts + GSAP │
                            └──────┬──────────┬────────┘
                                   │ HTTPS     │ WSS
                                   │ :8080     │ :8080/ws
                            ┌──────▼──────────▼────────┐
                            │  Nginx（静态 + 反代 + WS）  │
                            └──────────┬───────────────┘
                                       │ proxy_pass
                            ┌──────────▼───────────────┐
                            │  FastAPI (:8000)          │
                            │  ┌─────────────────────┐ │
                            │  │ 认证 & 鉴权          │ │
                            │  │ 拓扑 CRUD           │ │
                            │  │ 剧本 CRUD + 执行     │ │
                            │  │ 攻击引擎调度器       │ │
                            │  │ 防御引擎检查器       │ │
                            │  │ 抓包服务 (Scapy)     │ │
                            │  │ AI 文本→拓扑/剧本    │ │
                            │  │ 映射校验器           │ │
                            │  └─────────────────────┘ │
                            └──────┬──────┬────────────┘
                                   │      │
                    ┌──────────────┘      └──────────────┐
                    ▼                                    ▼
            ┌──────────────┐                  ┌──────────────────┐
            │  SQLite      │                  │  Redis (:6379)   │
            │  /app/data/  │                  │  Celery 任务队列   │
            └──────────────┘                  └──────────────────┘
                     ▲
                     │ docker network: mgmt (管理网)
                     │
═══════════════════════════════════════════════════════════════
                     │ docker network: simnet (仿真隔离网, 172.20.0.0/24)
                     ▼
            ┌──────────────────────────────────────────────────┐
            │  仿真节点池（按剧本动态创建/销毁）                  │
            │  ┌─────┐ ┌─────┐ ┌──────┐ ┌──────┐ ┌──────┐   │
            │  │ PC  │ │Web  │ │ DB   │ │Camera│ │WAF   │   │
            │  │靶机 │ │靶机 │ │靶机  │ │靶机  │ │防御  │   │
            │  └─────┘ └─────┘ └──────┘ └──────┘ └──────┘   │
            │  ┌─────┐ ┌─────┐ ┌──────┐ ┌──────┐            │
            │  │EDR  │ │Attkr│ │NGFW  │ │LLM   │  ...more   │
            │  │防御 │ │攻击机│ │防御  │ │护栏  │            │
            │  └─────┘ └─────┘ └──────┘ └──────┘            │
            └──────────────────────────────────────────────────┘
```

### 1.1 网络隔离策略（满足 H-01, H-08 硬约束）

| 网络 | 类型 | CIDR | 用途 |
|------|------|------|------|
| `mgmt` | bridge | 自动分配 | 管理面：Nginx↔Backend, Backend↔Redis, Backend↔Docker API |
| `simnet` | **`--internal`** bridge | `172.20.0.0/24` | 仿真数据面：靶机、攻击机、防御节点间通信，**禁出公网** |

`simnet` 通过 `docker network create --internal` 确保仿真流量永远无法到达外网，物理隔离保障。

---

## 2. 项目目录结构

```
demo-platform/
│
├── docker-compose.yml              # 一键启动编排（核心交付物）
├── .env.example                    # 环境变量模板（JWT_SECRET, LLM_API_KEY...）
├── Makefile                        # 开发便利命令
├── README.md
│
├── frontend/                       # ═══ 前端工程 ═══
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── index.html
│   └── src/
│       ├── main.ts                 # Vue 入口
│       ├── App.vue                 # 根组件
│       ├── router/index.ts         # 路由配置
│       ├── stores/                 # Pinia 状态管理
│       │   ├── auth.ts             #   认证状态
│       │   ├── topology.ts         #   拓扑编辑状态
│       │   ├── scenario.ts         #   剧本执行状态
│       │   ├── alerts.ts           #   告警实时状态
│       │   └── ui.ts               #   UI 全局状态
│       ├── composables/            # 可复用组合式函数
│       │   ├── useWebSocket.ts     #   WebSocket 连接管理
│       │   ├── useTopologyDrag.ts  #   拓扑拖拽逻辑
│       │   ├── usePacketStream.ts  #   实时报文流
│       │   └── useResponsive.ts    #   响应式断点检测
│       ├── api/                    # Axios API 封装
│       │   ├── client.ts           #   实例 + 拦截器
│       │   ├── auth.ts / topologies.ts / scenarios.ts
│       │   ├── executions.ts / alerts.ts / config.ts
│       ├── components/             # ═══ 组件树 ═══
│       │   ├── layout/             #   布局组件
│       │   │   ├── AppHeader.vue
│       │   │   ├── PcLayout.vue    #      PC 端三屏布局
│       │   │   └── MobileLayout.vue#      移动端 Tab 布局
│       │   ├── topology/           #   拓扑相关
│       │   │   ├── TopologyCanvas.vue      # AntV X6 画布
│       │   │   ├── NodePalette.vue         # 节点面板
│       │   │   ├── NodeDetailDrawer.vue    # 属性抽屉
│       │   │   ├── TopologyToolbar.vue     # 工具栏
│       │   │   └── TopologyMiniMap.vue     # 小地图
│       │   ├── scenario/           #   剧本相关
│       │   │   ├── ScenarioPlayer.vue      # 播放控制器
│       │   │   ├── AttackStepCard.vue      # 步骤卡片
│       │   │   ├── DefenseModeSwitch.vue   # 模式切换
│       │   │   └── ScenarioResult.vue      # 结果展示
│       │   ├── monitoring/         #   监控图表
│       │   │   ├── PacketStream.vue        # 实时报文流
│       │   │   ├── AttackTrendChart.vue    # ECharts 折线
│       │   │   ├── AttackTypePie.vue       # 饼图
│       │   │   ├── DefenseHeatMap.vue      # 热力图
│       │   │   ├── KillChainView.vue       # Kill Chain
│       │   │   └── AlertTable.vue          # 告警表格
│       │   ├── ai/                 #   AI 生成
│       │   │   ├── AiTextInput.vue
│       │   │   └── AiResultPreview.vue
│       │   └── common/             #   通用组件
│       │       ├── LoadingSpinner.vue
│       │       ├── StatusBadge.vue
│       │       └── ConfirmDialog.vue
│       ├── views/                  # ═══ 页面 ═══
│       │   ├── LoginView.vue
│       │   ├── DashboardView.vue           # 主仪表板（三屏联动）
│       │   ├── TopologyEditorView.vue      # 拓扑编辑器（全屏）
│       │   ├── ScenarioListView.vue        # 剧本列表
│       │   ├── ScenarioDetailView.vue      # 剧本编排
│       │   ├── ScenarioPlayView.vue        # 剧本播放（全屏演示）
│       │   ├── AlertCenterView.vue         # 告警中心
│       │   ├── PacketViewerView.vue        # 流量包查看
│       │   ├── AiGenerateView.vue          # AI 生成
│       │   ├── AdminUsersView.vue          # 用户管理
│       │   └── AdminAuditView.vue          # 审计日志
│       ├── styles/
│       │   ├── tokens.ts           #   设计 Token
│       │   ├── global.scss         #   全局样式
│       │   ├── dark-theme.scss     #   深色主题
│       │   └── responsive.scss     #   响应式
│       └── utils/
│           ├── formatters.ts
│           └── validators.ts
│
├── backend/                        # ═══ 后端工程 ═══
│   ├── Dockerfile
│   ├── pyproject.toml
│   ├── requirements.txt
│   └── app/
│       ├── main.py                 # FastAPI 入口 + 生命周期
│       ├── config.py               # pydantic-settings 配置
│       ├── database.py             # SQLAlchemy 引擎 + Session
│       │
│       ├── api/                    # ═══ 路由层 ═══
│       │   ├── deps.py             #   依赖注入
│       │   ├── v1/
│       │   │   ├── router.py       #   v1 路由聚合
│       │   │   ├── auth.py / config.py / topologies.py
│       │   │   ├── scenarios.py / executions.py
│       │   │   ├── packets.py / alerts.py
│       │   │   ├── users.py / audit.py / ai.py
│       │   └── ws/
│       │       ├── manager.py      #   WebSocket 连接管理器
│       │       └── handlers.py     #   WS 消息路由
│       │
│       ├── models/                 # ═══ ORM 模型 ═══
│       │   ├── user.py / topology.py / scenario.py
│       │   ├── execution.py / packet.py / alert.py / audit.py
│       │
│       ├── schemas/                # ═══ Pydantic Schema ═══
│       │   ├── common.py           #   统一响应 {code,message,data}
│       │   ├── auth.py / topology.py / scenario.py
│       │   ├── execution.py / alert.py / user.py
│       │
│       ├── services/               # ═══ 业务逻辑层 ═══
│       │   ├── auth_service.py
│       │   ├── topology_service.py
│       │   ├── scenario_service.py
│       │   ├── attack_engine.py    #   攻击引擎调度器 ★核心★
│       │   ├── defense_engine.py   #   防御引擎检查器 ★核心★
│       │   ├── capture_service.py  #   Scapy 抓包
│       │   ├── alert_service.py    #   告警聚合
│       │   ├── docker_service.py   #   Docker 节点管理
│       │   └── ai_service.py       #   LLM 集成
│       │
│       ├── engine/                 # ═══ 引擎核心 ═══
│       │   ├── registry.py         #   脚本自动发现与注册
│       │   ├── executor.py         #   执行器（线程池+超时）
│       │   ├── context.py          #   剧本执行上下文
│       │   └── mapping_validator.py#   映射校验器 ★关键★
│       │
│       ├── scripts/                # ═══ 预制脚本 ═══
│       │   ├── attacks/            #   30 个攻击脚本
│       │   │   ├── att_001_sqli_login_bypass.py
│       │   │   ├── att_002_sqli_dump.py
│       │   │   ├── ... (共30个)
│       │   │   └── att_030_vpn_brute.py
│       │   └── defenses/           #   27 个防御规则
│       │       ├── def_001_firewall_zone.py
│       │       ├── def_020_waf_sqli_xss.py
│       │       ├── ... (共27个)
│       │       └── def_027_llm_output_filter.py
│       │
│       └── tasks/                  # ═══ Celery 异步任务 ═══
│           ├── celery_app.py
│           └── scenario_task.py    #   剧本执行长任务
│
├── config/                         # ═══ JSON 静态配置 ═══
│   ├── node-types.json             #   节点类型字典
│   ├── attack-defense-mapping.json #   映射表（机器可读）★灵魂★
│   ├── topology-templates/         #   T-01 ~ T-05 预置模板
│   └── scenario-presets/           #   SCN-001 ~ 009 预置剧本
│
├── docker/                         # ═══ Docker 构建 ═══
│   ├── Dockerfile.backend
│   ├── Dockerfile.frontend
│   ├── Dockerfile.target-pc        #   靶机 PC 镜像
│   ├── Dockerfile.target-web       #   靶机 Web（含 DVWA 漏洞）
│   ├── Dockerfile.target-db        #   靶机数据库
│   ├── Dockerfile.attacker         #   攻击机（kali-lite）
│   ├── Dockerfile.defense          #   防御节点基础镜像
│   └── nginx.conf                  #   Nginx 配置
│
├── tests/                          # ═══ 测试 ═══
│   ├── conftest.py
│   ├── backend/
│   │   ├── test_auth.py / test_topology.py / test_scenario.py
│   │   ├── test_attack_engine.py / test_defense_engine.py
│   │   ├── test_mapping_validator.py / test_ai_service.py
│   └── frontend/                   #   Vitest
│       ├── TopologyCanvas.test.ts / ScenarioPlayer.test.ts
│       └── composables.test.ts
│
└── scripts/                        # ═══ 运维脚本 ═══
    ├── init_data.py                #   初始化种子数据
    ├── load_presets.py             #   加载预置剧本
    ├── reset_targets.py            #   重置靶机
    └── health_check.py             #   健康检查
```

---

## 3. 核心模块设计

### 3.1 后端服务分层与依赖关系

```
                   FastAPI Router (api/v1/*)
                          │
                   ┌──────┴──────┐
                   │   deps.py   │  (get_db, get_current_user)
                   └──────┬──────┘
                          │
          ┌───────────────┼───────────────┐
          │               │               │
    ┌─────▼─────┐  ┌──────▼──────┐  ┌─────▼─────┐
    │Topology   │  │Scenario     │  │Alert      │
    │Service    │◄─┤Service      │──┤Service    │
    └─────┬─────┘  └──────┬──────┘  └─────┬─────┘
          │               │               │
          │        ┌──────▼──────┐        │
          │        │AttackEngine │        │
          │        │ (调度器)     │────────┤
          │        └──────┬──────┘        │
          │               │               │
          │        ┌──────▼──────┐        │
          │        │DefenseEngine│────────┤
          │        │ (检查器)     │────────┤
          │        └──────┬──────┘        │
          │               │               │
    ┌─────▼─────┐  ┌──────▼──────┐  ┌─────▼─────┐
    │Capture    │  │Mapping      │  │AI          │
    │Service    │  │Validator    │  │Service     │
    └───────────┘  └─────────────┘  └────────────┘
          │
    ┌─────▼─────┐
    │WebSocket  │
    │Manager    │
    └───────────┘
```

### 3.2 模块间通信方式

| 通信方式 | 场景 | 实现 |
|---------|------|------|
| REST API | CRUD 操作 | FastAPI + Pydantic |
| WebSocket | 实时事件：包/告警/步骤状态 | FastAPI WebSocket + connection manager |
| Celery + Redis | 长时任务：剧本执行 | Celery task → WS 推送进度 |
| Docker API | 仿真节点生命周期 | docker-py 通过 /var/run/docker.sock |
| 文件 I/O | pcap / JSON 配置 | 同步 I/O（小文件），异步 I/O（pcap） |

---

## 4. 核心数据流设计

### 4.1 剧本执行主流程（端到端）

```
用户点击 [启动剧本]
       │
       ▼
POST /api/v1/scenarios/{id}/start
       │
       ▼
ScenarioService.start(scenario_id, speed)
       │
       ├── 1. MappingValidator.validate_scenario(scenario)
       │      └── 返回 issues[] → 警告但不阻断
       ├── 2. 创建 ScenarioExecution 记录（status=pending）
       ├── 3. 提交 Celery 任务 execute_scenario.delay(execution_id)
       │
       ▼
┌── Celery Task: execute_scenario ──────────────────────────┐
│                                                            │
│  3.1 更新状态 → running, WS broadcast "execution_started"  │
│  3.2 DockerService.deploy_topology(topology_id)            │
│      └── 根据拓扑 JSON 动态创建 Docker 容器                  │
│  3.3 CaptureService.start(scenario_id)                     │
│      └── AsyncSniffer 绑定 simnet 网桥                     │
│                                                            │
│  3.4 FOR EACH step IN scenario.attack_sequence:            │
│       │                                                    │
│       ├── WS broadcast "step_start"                        │
│       ├── 创建 StepExecution 记录（status=running）          │
│       │                                                    │
│       ├── DefenseEngine.check(request_data, defense_config) │
│       │   └── 遍历所有启用的防御规则                         │
│       │       ├── 命中 → result.status = "blocked"         │
│       │       │        → AlertService.create_alert()       │
│       │       │        → WS "attack_blocked"               │
│       │       └── 未命中 → AttackEngine.run_step()          │
│       │                   → 加载脚本 → 执行 → 返回结果       │
│       │                                                    │
│       └── 保存 StepExecution → WS "step_complete"          │
│                                                            │
│  3.5 CaptureService.stop()                                 │
│  3.6 汇总 → status = "success" → WS "scenario_complete"    │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### 4.2 WebSocket 实时推送架构

```
Scapy AsyncSniffer             Celery Task               Defense Engine
       │                           │                          │
   抓包事件                     步骤进度                     告警事件
       │                           │                          │
       ▼                           ▼                          ▼
  ┌─────────────────────────────────────────────────────────────┐
  │                 WebSocket Connection Manager                  │
  │                                                               │
  │  topics:                                                      │
  │  • ws://host/ws?topic=packets:{exec_id}   实时包摘要           │
  │  • ws://host/ws?topic=scenario:{exec_id}  步骤状态+攻击结果    │
  │  • ws://host/ws?topic=alerts:{exec_id}    实时告警             │
  │  • ws://host/ws?topic=topology:updates    拓扑变更（协同编辑）  │
  └────────────────────────┬────────────────────────────────────┘
                           │
                           ▼
                 前端 useWebSocket()
           (自动重连 + ping/pong + JSON 解析)
```

### 4.3 前端 Pinia 核心 Store 设计

```typescript
// stores/scenario.ts — 最重要的 Store
interface ScenarioState {
  currentExecution: ScenarioExecution | null
  stepResults: Map<string, StepResult>     // step_id → result
  alerts: Alert[]
  packets: PacketEvent[]
  defenseMode: 'none' | 'partial' | 'full'
  enabledProducts: Set<string>
  playStatus: 'idle' | 'starting' | 'running' | 'stopping' | 'completed'
  executionStats: {
    totalSteps: number
    completedSteps: number
    blockedSteps: number
    successSteps: number
  }
}
```

---

## 5. 前端组件树与路由

### 5.1 路由表

| 路径 | 页面 | 权限 | 说明 |
|------|------|------|------|
| `/login` | LoginView | 公开 | 登录页 |
| `/` | DashboardView | 登录 | 三屏联动主仪表板 |
| `/topology/:id?` | TopologyEditorView | admin | 拓扑编辑器（全屏） |
| `/scenarios` | ScenarioListView | 登录 | 剧本列表 |
| `/scenarios/:id` | ScenarioDetailView | 登录 | 剧本编辑 |
| `/scenarios/:id/play` | ScenarioPlayView | 登录 | 演示播放（全屏） |
| `/alerts` | AlertCenterView | 登录 | 告警中心 |
| `/packets` | PacketViewerView | 登录 | 流量查看器 |
| `/ai-generate` | AiGenerateView | admin | AI 剧本生成 |
| `/admin/users` | AdminUsersView | admin | 用户管理 |
| `/admin/audit` | AdminAuditView | admin | 审计日志 |

### 5.2 PC 端三屏布局组件树

```
PcLayout.vue
├── AppHeader.vue
│   ├── Logo + 平台名
│   ├── ScenarioSelector (下拉切换剧本)
│   ├── DefenseModeSwitch.vue (none/partial/full)
│   ├── PlayControl.vue (▶启动 ■停止)
│   └── UserMenu.vue (头像 + 角色 + 退出)
│
├── <三屏区域 - 可拖拽分栏>
│   ├── LeftPane (50%) — TopologyCanvas.vue
│   │   ├── AntV X6 Graph 实例
│   │   ├── NodePalette.vue (左侧浮动面板)
│   │   ├── TopologyToolbar.vue (顶部浮动工具栏)
│   │   └── TopologyMiniMap.vue (右下角)
│   │
│   ├── CenterPane (25%) — 策略配置区
│   │   ├── DefenseProductList.vue
│   │   │   └── DefenseProductCard.vue × 8
│   │   │       ├── Toggle 开关
│   │   │       └── 规则子列表（可展开）
│   │   └── AttackStepList.vue (当前步骤高亮)
│   │       └── AttackStepCard.vue × N
│   │
│   └── RightPane (25%) — 设备详情区
│       ├── NodeDetailDrawer.vue (选中节点属性)
│       ├── AlertStream.vue (实时告警滚动)
│       └── AttackTrendChart.vue (ECharts 折线)
│
├── PacketStream.vue (底部 120px，可折叠)
│   └── PacketRow.vue × N (虚拟滚动)
│       ├── 时间 | 源IP:端口 → 目的IP:端口 | 协议
│       ├── 攻击标签 (红色) / 拦截标签 (绿色)
│       └── 长度 + 标志位
│
└── StatusBar.vue (连接状态 / FPS / 当前时间)
```

### 5.3 移动端 Tab 切换布局

```
MobileLayout.vue (≤768px)
├── AppHeader.vue (简化：Logo + 剧本名)
├── TabBar.vue (拓扑 / 策略 / 详情 / 报文)
├── <Tab 内容区>
│   ├── TopologyCanvas.vue (pinch 缩放 + 单指拖动)
│   ├── DefenseProductList.vue (折叠面板)
│   ├── AlertStream.vue
│   └── PacketList.vue (滚动列表)
└── BottomBar.vue (固定底部)
    ├── ▶ 启动 / ■ 停止 (大按钮)
    └── DefenseModeSwitch (小型 toggle)
```

### 5.4 交互动效清单

| 动效 | 触发条件 | 实现 |
|------|---------|------|
| 节点脉冲 | 节点被选中 | GSAP scale 1→1.05→1 |
| 攻击光效 | 攻击步骤执行 | GSAP 红色光圈从攻击机扩散到目标 |
| 防御光环 | 防御规则命中 | GSAP 绿色光环从防御节点扩散 |
| 流量粒子 | 数据包传输 | X6 edge animation（沿连线移动的圆点） |
| 背景粒子 | 持续 | Three.js 星空/矩阵数字粒子 |
| 页面切换 | 路由跳转 | Vue Transition (fade+slide) |
| 数字滚动 | 统计数据更新 | ECharts 动画 / countUp |
| 攻击冲击 | critical 告警 | CSS shake + 红色边框闪烁 |

---

## 6. 关键接口设计

### 6.1 攻击脚本接口规范

每个攻击脚本 `.py` 必须导出：

```python
# 元数据（平台启动时注册）
SCRIPT_META: dict = {
    "id": "ATT-001",
    "name": "SQL注入-登录绕过",
    "version": "1.0.0",
    "category": "web_vuln",
    "mitre_tactic": "TA0001",
    "severity": "high",
    "target_node_types": ["web-server", "app-server"],
    "blockable_by": ["waf", "edr-server"],  # ★严格按映射表★
}

# 执行函数
def run(target_url: str, params: dict, context: dict) -> dict:
    """返回 {status, logs, evidence, traffic_generated}"""
```

### 6.2 防御规则接口规范

```python
RULE_META: dict = {
    "id": "DEF-020",
    "name": "WAF - SQL/XSS/上传拦截",
    "product": "waf",
    "blocks_attacks": ["ATT-001","ATT-002","ATT-003","ATT-004","ATT-005"],
}

def check(request: dict, config: dict) -> dict | None:
    """返回 None 放行，返回 dict 则阻断并告警"""
```

### 6.3 映射校验器接口

```python
class MappingValidator:
    def validate_scenario(scenario: dict) -> list[str]:
        """返回问题列表，空列表表示通过"""
    
    def get_recommended_defense(attack_id: str) -> dict:
        """根据攻击 ID 推荐防御产品配置"""
```

---

## 7. 关键技术决策

| # | 决策点 | 选择 | 理由 |
|---|--------|------|------|
| D1 | 前端目录 | 按功能分模块 | 团队协作友好，AI 代码生成好定位 |
| D2 | 攻击脚本形式 | Python 模块 | 攻击逻辑复杂（网络I/O、多步骤），Python 表达能力远超 JSON |
| D3 | 防御规则形式 | Python 函数 | 同 D2，未来可引入 ML 模型 |
| D4 | 拓扑渲染 | AntV X6 (Canvas) | 节点数可能 100+，SVG DOM 开销大 |
| D5 | 实时通信 | WebSocket | 双向 + 低延迟，FastAPI 原生支持 |
| D6 | 长任务 | Celery + Redis | 监控/重试/进度跟踪成熟 |
| D7 | 仿真节点 | Docker 动态创建 | 节省资源，按剧本启动 |
| D8 | AI 调用 | 后端调 LLM | API Key 安全，后端实体校验兜底 |
| D9 | 架构模式 | 模块化单体 | 单机演示场景（1-10 并发），微服务是负收益 |
| D10 | 数据库 | SQLite 单文件 | 需求硬约束 H-05 |

---

## 8. 开工顺序（分 5 个 Phase）

### Phase 1: 基础设施 + 最小闭环（~15 人天）

```
1.1  后端脚手架      fastapi + SQLAlchemy + SQLite + Docker
1.2  前端脚手架      Vue3 + Vite + Element Plus + 路由
1.3  认证模块        JWT + 角色
1.4  数据模型        全部表建完
1.5  配置 API        node-types / attack-scripts 元数据查询
1.6  拓扑服务        CRUD + JSON 序列化
1.7  拓扑前端        AntV X6 画布 + 拖拽 + 属性面板
1.8  攻击引擎        registry + executor + 1个示例 (ATT-001)
1.9  防御引擎        简易检查 + 1条规则 (DEF-020)
1.10 最小演示        SCN-001 硬编码步骤 → 端到端跑通
```

**验证标准**：打开浏览器 → 登录 → 看到拓扑 → 点"启动" → 看 SQL 注入日志 → 开 WAF → 看"已拦截"

### Phase 2: 脚本库 + 引擎完善（~15 人天）

```
2.1  ATT-001~030 全部攻击脚本
2.2  DEF-001~027 全部防御规则
2.3  映射校验器完整版
2.4  WebSocket 推送（三路 topic）
```

### Phase 3: 前端完整功能（~20 人天）

```
3.1  Dashboard 三屏联动
3.2  剧本编辑器（拖拽编排）
3.3  剧本播放器（全屏演示模式）
3.4  日志可视化（6 类图表）
3.5  pcap 查看器
3.6  Kill Chain 视图
3.7  动效（GSAP + Three.js）
3.8  用户管理 + 审计日志
```

### Phase 4: AI 集成 + 移动端（~10 人天）

```
4.1  AI 文本→拓扑/剧本 API
4.2  AI 生成前端页面
4.3  移动端响应式适配 + 手势
```

### Phase 5: 部署 + 测试 + 文档（~8 人天）

```
5.1  Docker Compose 编排最终化
5.2  预置数据加载脚本
5.3  集成测试 + 性能测试
5.4  部署手册 + 演示员培训材料
```

**总计**：~68 人天（需求估算 113 人天，设计优化后可压缩至 68 左右）

---

## 9. 需求追溯矩阵

| 需求编号 | 需求 | 设计覆盖位置 |
|---------|------|-------------|
| H-01 | 攻击物理隔离 | §1.1 `--internal` 网络 |
| H-02 | 映射表严格遵循 | §6.3 映射校验器 |
| H-03 | Docker 一键启动 | §2 docker-compose.yml |
| H-04 | PC/移动双端 | §5.2 PC布局 + §5.3 移动布局 |
| H-05 | SQLite | §2 data/data.db |
| H-06 | 节点类型可扩展 | §config/node-types.json |
| H-07 | UI 炫酷 | §5.4 动效清单 + §5.2 三屏联动 |
| H-08 | 纯内网 | §1.1 simnet isolation |
| M-01~M-10 | 10 个功能模块 | §3-§5 |
| D-04~D-08 | 预制脚本/规则/剧本/模板/映射表 | §2 config/ + scripts/ |

---

## 10. 风险与降级策略

| 风险 | 可能性 | 缓解 | 降级方案 |
|------|-------|------|---------|
| AntV X6 移动端兼容差 | 中 | 先做 PC，移动端评估后再定 | 移动端用静态 SVG + CSS 动画 |
| Scapy 在 macOS/Win Docker 受限 | 中 | 容器内抓 simnet 流量 | `docker exec tcpdump` 替代 |
| 30 攻击脚本开发超预期 | 中 | Phase 1 先做 6 个核心，其余按需 | 按类别缩减至 ~15 个 |
| LLM API 离线不可用 | 低 | AI 生成本是可选项 | 禁用按钮 + 提示"需联网" |
| 攻击-产品映射出错 | 中 | MappingValidator 启动时+保存时强校验 | N/A（不允许降级） |

---

> **设计文档结束 | 2026-07-01 | V1.0**  
> **作者**：基于需求文档自动生成  
> **下一步**：请审阅设计 → 确定是否进入 Phase 1 PoC 开发
